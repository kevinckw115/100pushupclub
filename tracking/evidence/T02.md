# T02: design components

Branch task/t02-design-components. 2026-09-10. Plan: implement token-based shell, ring, buttons, quantity controls, sheet, rows and tab bar; verify interactive browser rendering and types.

Shared components use canonical tokens, system fonts, 48px controls, reduced-motion handling, and large-text count layout. The illustrative component gallery is gated by __DEV__ and isolated under src/testing.

Checks: mobile typecheck passed; lint passed with one warning for intentional development-only require. Web dev export passed. node scripts/review-components.mjs passed ring 35/100 labels, preset replacement, decrement, sheet dismissal and selected-tab accessibility checks at 390x844 and 320x640. Screenshots t02-35-web.png, t02-100-web.png, t02-sheet-web.png, t02-compact-web.png inspected. Fixed quantity input overflow, SVG web property warnings, and explicit tab accessibility labels/state after actual browser failures. Browser test uses reduced motion for stable captures.

Native system text at 200%, screen readers, safe areas and keyboard are not verified. T04 may use components while that independent device gate stays open.
