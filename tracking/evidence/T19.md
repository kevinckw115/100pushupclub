# T19: deletion, export and support

9453e69 pushed for connected verification. Local ordinary web export and an actual guest JSON download passed with no browser errors; t19-export-web.png inspected. All54 integration tests, lint and typecheck passed. T18 final877e64e now passed all CI.

5cb5511 passed the new real deletion/export/Auth/worker suite and34 SQL assertions in backend34620795916; overall CI then failed the already-fixed T18 background-screen selector. Its mobile34620795941/package34620795832 passed. Client now includes fresh existing-account-only OTP deletion, durable proof/cleanup markers, guest preservation, private local/cloud JSON export and policy/support routes. Four new SQLite/parser tests pass (54 total integration); types/lint pass. Native export streams into a private cache file and removes it after sharing; interrupted exports are removed at startup. Web export downloads a Blob without uploading it (100MB preview limit). Cloud export holds the cursor through server rate pauses and explicitly restarts if account records change. Connected browser verification pending. Owner support email/domain requested asynchronously; left unconfigured.

SDK57 sources checked: https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/ and https://docs.expo.dev/versions/v57.0.0/sdk/sharing/ . Existing lockfile includes supported expo-file-system~57.0.7 and expo-sharing~57.0.19. Native sharing/export/lifecycle remains unverified.

Branch task/t19-deletion-export, stacked on T18 d44389f. Circle browser verification is running; backend deletion/export work is independent of its remaining UI checks.

Plan: recent verified authentication and explicit deletion consent; transactionally disable account/public visibility, invalidate epochs and revoke sessions before queuing bounded retryable cleanup. Transfer owned circles to earliest eligible member (UUID tie-break), delete if none, remove account data and Auth identity. A random status capability returns only job state after Auth removal, so a lost response/restart can recover without recreating the deleted account. Export bounded own records/preferences with concurrent-change detection; local guest export remains separate. Add in-app and verified web request surfaces, accurate policy/setup/support states, no invented contact or published site. Test stale JWT/refresh, rollback, owner cleanup/races, worker retries and export peer exclusion. Hosted scheduling, contact/retention details and native deletion/export remain setup gates.

CI a1cd868 passed backend authorization, exports and circle flows but exposed repeated deletion-status cleanup resetting navigation. Fixed acknowledgement to create cleanup marker only for first acceptance/current account, and Auth cleanup to return when already completed. Real SQLite regression preserves guest reminders and cleared marker across repeated status updates;4 privacy tests passed. Connected browser completion assertion remains unchanged.
