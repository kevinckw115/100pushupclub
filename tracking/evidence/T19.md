# T19: deletion, export and support

Branch task/t19-deletion-export, stacked on T18 d44389f. Circle browser verification is running; backend deletion/export work is independent of its remaining UI checks.

Plan: recent verified authentication and explicit deletion consent; transactionally disable account/public visibility, invalidate epochs and revoke sessions before queuing bounded retryable cleanup. Transfer owned circles to earliest eligible member (UUID tie-break), delete if none, remove account data and Auth identity. A random status capability returns only job state after Auth removal, so a lost response/restart can recover without recreating the deleted account. Export bounded own records/preferences with concurrent-change detection; local guest export remains separate. Add in-app and verified web request surfaces, accurate policy/setup/support states, no invented contact or published site. Test stale JWT/refresh, rollback, owner cleanup/races, worker retries and export peer exclusion. Hosted scheduling, contact/retention details and native deletion/export remain setup gates.
