# T12: guest import

Branch task/t12-guest-import. Plan: explicit selection/consent, bounded batches of50, immutable source snapshots and stable mutation/destination mapping, atomic account outbox writes, restart/replay, collision decisions and verified cleanup. Guest history remains unchanged until a separate cleanup choice. Imported records retain recorded time and source=import with no public metadata, excluding them from circle history by contract.

Files: SQLite migration4/import repository, worker acknowledgment/collision integration, import screen and Settings entry, real SQLite and backend tests. Verification: repeated import and process restart, interrupted acknowledgment, account with existing history, same/different-owner UUID collisions, source changes before cleanup, no implicit second-account import. T11 native lifecycle gate is independent of this storage/HTTP work and remains open.

Implemented: selected private imports, stable replay mapping, immutable source snapshots, automatic bounded other-owner collision remapping, explicit same-owner conflicts, pause/resume/stop retry after signout and optional verified guest cleanup. Account response snapshots are purged at signout; the guest deduplication ledger remains. Account import copies cannot be edited before acceptance.

Local automated checks: npm.cmd run typecheck; npm.cmd run lint; npm.cmd test (16 pass); npm.cmd run test:integration (33 pass, including7 import invariants); npm.cmd run doctor (21/21); npm.cmd run export:web (pass, excludes test adapters). Backend test added for real two-owner UUID collisions, partial import/response loss, SQLite file reopen, exact replay, repeated import and source/public metadata; it awaits CI. Real connected browser selection/consent and unchanged guest restoration are added, with a screenshot artifact for review.

Compatibility repair: Expo's September11 SDK57 recommendation advanced to57.0.22 and companion patch releases. Ran node node_modules/expo/bin/cli install --fix twice: first updated Expo but its npx reentry hit Windows Access denied; direct Node reentry completed. Single mobile lockfile retained, no unrelated major upgrades. Revalidated and renamed the unchanged web bridge patch for expo-sqlite57.0.3; exact installed patch-version validation now runs before postinstall. Native exports will be rechecked in CI. Native device/hosted OTP gates remain open.
