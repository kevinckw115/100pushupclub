# T15: Club interface and foreground refresh

Branch task/t15-club-interface, stacked on T14 0d5ecc6. T14 backend contract passed at1ab8b80; client checks pass locally and connected browser/full export CI remains running. T15 is independent UI/read work against that tested contract.

Plan: replace placeholder with World/selected-region scope controls, actual fallback scope, exact 24-hour total and one-hour people labels, sanitized chronological rows and empty/error states. Use a bounded in-memory controller with identity/scope fencing, 30-second foreground polling, 10-second request floor and Retry-After. No realtime subscription is configured, so use Recent check-ins, never Live. Stop on blur/background/offline; reconnect refetches. Avoid public disk caches because privacy purge is not yet proven. Refreshed pages remove hidden rows and update existing quantities immediately, but new rows wait behind an explicit show-updates action to preserve scrolling. Cap loaded rows and reset stale cursors safely.

Verification: strict projection parser, late-response identity/scope fencing, bounded coalesced refresh/pagination, rate/backoff and foreground controls, visibility removal without automatic prepend, real two-account connected UI, offline/error/unconfigured states, ordinary exports. Native A25 lifecycle/scroll checks remain a device gate.

Implemented reader and Cream & Brick screen. Six deterministic reader/parser tests pass; local TypeScript and lint pass. Public transport validates and strips unknown projection fields, attaches the verified session when present, refreshes authentication at most once, bounds payload/timeouts and respects server retry delays. Two real account fixtures drive the added connected browser check for totals, scrolling, opt-out removal, offline clearing, region fallback and stopped background polling. No test fixtures are bundled in the app. Connected CI verification is pending.
