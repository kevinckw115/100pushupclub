# T05: log, edit, delete and Undo

Branch task/t05-log-edit-undo. 2026-09-10. Plan: durable logging sheet with validation/presets, >100 confirmation, locked save attempts, corrections and eight-second Undo. Exercise real SQLite and browser UI including a write-failure trigger.

Implemented: no success before commit, retained draft on failure, quantity-only correction, tombstone deletion, single screen-reader announcement after save, and Undo of unsent creates or dependent deletion behind sent creates. The network worker remains T11, so queued dependency behavior is tested locally without claiming server convergence. Development failure harness creates a real SQLite trigger and is gated out of release routes.

Checks: npm.cmd run test:integration passed 11 checks; typecheck/lint passed (two intentional development-only require warnings). Web development export and node scripts/review-logging.mjs --failure passed: 20+15=35 after reload, two immediate save clicks produce one effect, invalid input stays open, totals 100 and125, Undo, quantity edit, deletion, >100 confirmation/cancel, failing SQL write retains input and existing total with no success, disarm/retry saves. Screenshots t05-today-35-web.png, t05-today-100-web.png, t05-today-125-web.png and t05-storage-failure-web.png inspected.

This test exposed expo-sqlite 57.0.2 web worker length-header truncation above255 bytes. A one-line patch writes the full32-bit header; patch-package applies it on install. See apps/mobile/patches/README.md. The same previously failing browser flow passed after the patch. Native SQLite is unaffected by this web bridge patch. Clean-install patch verification is recorded below when complete.

Maestro local-tracker flow added but not run: native target unavailable. T06/T07 can proceed independently while native gates remain open.

Clean install: npm.cmd ci passed (842 packages); postinstall patch-package8.0.1 applied expo-sqlite@57.0.2 successfully.
