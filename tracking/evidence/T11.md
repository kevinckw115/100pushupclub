# T11: client synchronization

Branch task/t11-client-sync. Plan: separate production SyncRepository, strict HTTP DTO validation, one worker per verified account lease, durable sending/retry/dependencies, accepted snapshots with optimistic projection, atomic pull/cursor, conflict resolution and truthful sync UI. Network/foreground events start work; background/signout invalidate requests.

T09 native storage gate remains open; the worker/real SQLite/real backend tests are independent and continue under the documented native-gate exception. Expo Network57.0.1 installed through Expo compatibility installer. No IP/MAC address collection; connectivity state only.

Verification planned: actual SQLite lost-response replay, in-flight Undo, cursor rollback, restart, stale identity response, validation/401/rate handling, and two real backend clients with concurrent edits/deletion. UI browser checks supplement, never replace, native verification.
