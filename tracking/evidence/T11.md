# T11: client synchronization

Branch task/t11-client-sync. Plan: separate production SyncRepository, strict HTTP DTO validation, one worker per verified account lease, durable sending/retry/dependencies, accepted snapshots with optimistic projection, atomic pull/cursor, conflict resolution and truthful sync UI. Network/foreground events start work; background/signout invalidate requests.

T09 native storage gate remains open; the worker/real SQLite/real backend tests are independent and continue under the documented native-gate exception. Expo Network57.0.1 installed through Expo compatibility installer. No IP/MAC address collection; connectivity state only.

Verification planned: actual SQLite lost-response replay, in-flight Undo, cursor rollback, restart, stale identity response, validation/401/rate handling, and two real backend clients with concurrent edits/deletion. UI browser checks supplement, never replace, native verification.

Implemented: production worker/HTTP transport, durable retry and conflict repository, SQLite migration3, account lease lifecycle, Today/Settings notices and explicit conflict screen. No hosted service was deployed.

Automated evidence at05940d6: [backend run](https://github.com/kevinckw115/100pushupclub/actions/runs/34545907318) passed fresh migrations, 11 SQL authorization assertions, real JWT checks and two production SQLite clients using real HTTP RPCs. The clients verified response loss followed by full database reopen, exact receipt replay, in-flight Undo, concurrent conflicting edits, explicit reapply, deleted-record replacement and convergence. Connected OTP browser flow passed. [Mobile run](https://github.com/kevinckw115/100pushupclub/actions/runs/34545907342) passed lint/types/unit/integration/doctor and full Android/iOS Hermes plus web export.

Local commands (apps/mobile): npm.cmd run typecheck; npm.cmd run lint; npm.cmd test (16 pass); npm.cmd run test:integration (23 pass at05940d6); npm.cmd run export:web; node scripts/review-logging.mjs; node scripts/review-startup.mjs; npm.cmd run doctor (21/21). Follow-up tests add actual HTTP 401 refresh/429/token fencing, foreground pause/coalescing and v2 acknowledgment migration. Extended connected browser scenario now checks offline account save and reconnection; awaiting its CI run.

Device-verified: none. Native lifecycle/network/secure-storage behavior needs an installed development build; browser and host SQLite results do not satisfy those gates. Next: final follow-up CI, then T12 guest import on a separate branch; import storage/protocol work is independent of the missing native gate.
