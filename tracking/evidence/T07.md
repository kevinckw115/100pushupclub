# T07: preferences and local reminders

Branch task/t07-preferences-reminders. 2026-09-10. Plan: durable device preferences, optional haptics only after commit, native permission flow and seven one-shot reminders reconciled on settings/foreground/goal changes.

Implemented: haptic preference, reminder time validation, explicit enable/disable, denial guidance, scheduling-failure messages that distinguish committed preference from OS scheduling, deterministic identifiers, serialized reconciliation and restart-safe partial scheduling recovery. Default reminder18:00 remains disabled until enabled; haptics default on with silent unsupported fallback. Reconciliation retains matching future requests and cancels only stale/disabled/completed-day owned reminders. It never requests a remote push token.

Checks: npm.cmd test passed11 unit checks; reminder cases cover DST gap/repeat, changing wall time, denied permission, disabling without permission request, repeated reconciliation, completion-day cancellation, and partial-failure recovery. OS adapter in these unit tests is explicitly a stand-in: not native permission/delivery evidence. Typecheck/lint passed; Expo Doctor21/21. Production web export and node scripts/review-settings.mjs passed real SQLite preferences after reload, invalid time rejection and truthful unsupported-browser reminder UI. t07-settings-web.png inspected.

Bundle inspection found __DEV__ guards alone left review modules in Metro output. Review routes are now generated temporarily by npm run build:review and removed after export. npm run export:web scans the ordinary bundle and passed absence checks for fixture/failure modules and node:sqlite. Review build + node scripts/review-logging.mjs --failure passed and cleanup confirmed. Lint now has no warnings.

Native A33 remains not run, as do haptic and permission UI checks. No physical-delivery or native-offline claim. Next T08: backend schema/security and disposable real Supabase checks.
