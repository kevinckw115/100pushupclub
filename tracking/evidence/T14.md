# T14: consent and public queries

Branch task/t14-consent-public-queries. Plan: checked idempotent profile/alias/region/sharing updates with expected epoch; opaque actor/entry IDs; encrypted, integrity-protected feed cursors; one current eligibility predicate for rows/counts/global sparse-scope threshold; bilateral viewer blocks; no shared response cache. Add durable pending privacy UI and attach only confirmed current epochs to new native logs. Consent changes remain visibly pending offline.

Verification: real nine/ten-user fixtures, epoch toggle/region changes and concurrent creates, import/deletion/moderation/suspension exclusion, current quantity aggregates and distinct owners, ciphertext/DTO/private-ID inspection, cursor scope/viewer/tamper/expiry checks, explicit grants and real JWT cases. Local SQLite privacy request persistence plus connected UI; native privacy/network checks remain open. No hosted backend deployment. Directory dependency T13 verified at227a217.

Backend commit 1ab8b80 passed Actions backend 34613742725, mobile 34613742727 and package 34613742714. This includes real Postgres delayed-commit pagination: a transaction invisible at page one cannot enter subsequent pages, while current consent/block/deletion/moderation rules are reapplied. Public actor/entry identifiers are independent random IDs; cursors are authenticated encrypted payloads with viewer/scope binding and 15-minute expiry. Private creation transaction metadata never reaches a public DTO.

Client implementation adds SQLite-backed immutable pending profile requests, serial profile-before-check-in sync, current-profile confirmation after receipt replay, durable retry deadlines, explicit conflict resolution and sign-out protection. Sharing form preserves drafts across navigation/restart; alias-only changes omit consent fields. New logs request only a confirmed enabled epoch with no pending profile change; guest imports remain private. Four real SQLite tests cover reopen/replay, atomic receipt/cache failure, conflict choice, rate/clock rollback and late sign-out responses. Local integration 38/38 and lint passed; connected browser and native export rerun pending.

Limitations: browser verification does not establish native process-kill/network behavior. Anonymous feed gateway rate enforcement and hosted infrastructure remain deployment gates; authenticated public reads and profile changes have database rate budgets. No hosted service was mutated.

0d5ecc6 mobile Actions34614633450 and package34614633464 passed. Backend34614633552 caught an outdated import fixture that directly wrote region_id='world', bypassing the profile contract's null-for-World normalization. Fixture now enables sharing through the real checked update_profile RPC; all import collision/replay/privacy assertions remain. Backend/connected UI rerun required.
