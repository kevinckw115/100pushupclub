# T03: domain and SQLite

Branch task/t03-domain-sqlite. 2026-09-10. Plan: production validation, versioned schema, bound-parameter repository, atomic account create/outbox, guest partition lifecycle. Verify real SQLite close/reopen, process exit, rollback, invalid data, and migrations.

Implemented: production Expo SQLite driver and separate Node SQLite host-test adapter; no test identity or data imported by production modules. Two schema migrations establish partitions, check-ins, outbox, preferences, cursors/import/cache tables and indexes. Accepted snapshots stay separate from projected values. All account create/outbox writes share one SQLite transaction; guest writes do not create cloud work. Local partition IDs are storage scope, not backend authorization. T09 must derive account partitions from verified auth.

Commands in apps/mobile: npm.cmd test (5 passed); npm.cmd run test:integration (8 passed, including actual SQLite file close/reopen, separate process exit without close, injected outbox failure rollback, cross-partition isolation, SQL invariants, v1-to-v2 preservation and failed migration rollback); npm.cmd run typecheck (passed); npm.cmd run lint (passed, preexisting dev-gallery require warning); npm.cmd run doctor (21/21). Node 24.12.0 built-in SQLite executes the same schema and repository used by the native driver. Expo-sqlite installed through compatibility installer and plugin added explicitly to app.json after dynamic-config notice.

T03 host automated gate verified. Native Expo adapter has not yet been run on a device; native durability remains a separate T04/T05 gate. No UI introduced in this task. Next T04 can proceed independently of T02 native visual review.
