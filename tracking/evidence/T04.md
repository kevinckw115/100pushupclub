# T04: guest onboarding and Today

Branch task/t04-guest-today. 2026-09-10. Plan: initialize actual Expo SQLite, durable guest setup, active partition provider, Today totals/rows and four-tab navigation. Verify onboarding offline after browser asset load, reload persistence and routing; leave native first-launch gate explicit.

Implemented welcome, Today, tabs, storage loading/failure states and settings route. Connected tabs disclose unavailable services without invented activity or inert action buttons. Gallery moved to a development-only route. SQLite opens asynchronously before bounded synchronous repository transactions; this fixed a real browser worker initialization timeout. Bundled icon font preloads before first-run interaction so offline navigation does not trigger a late browser font request.

Checks: production web export passed; node scripts/review-guest.mjs passed actual Expo SQLite guest commit with browser offline after asset load, reload persistence, Club/Today/settings navigation, and no page errors. Screenshots t04-welcome-web.png and t04-today-zero-web.png inspected. Typecheck/lint passed (development-gallery require warning remains).

Native cold-start offline not run. Browser reload requires the preview server for app assets, so it is not evidence of native first launch offline. 35/100/125 states will be verified using real saves in T05. T05 can proceed independently of the missing native gate.
