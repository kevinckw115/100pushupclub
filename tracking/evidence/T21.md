# T21: resilience and performance

Branch task/t21-resilience-performance, stacked on T20 21434b2. T20 native checks are blocked, but this task's storage/backend work is independent. T19 connected browser verification is still running.

Plan: abrupt process termination inside an uncommitted SQLite transaction;100k local records with indexed day/history and bounded export; real PostgreSQL migration from populated baseline schema within a rollback-isolated transaction;100k backend records and100 foreground client reads with realistic staggered polling. Record p95 and dataset shape without claiming representative native/deployment-region timing. Preserve existing privacy/concurrency/identity/fault tests. No telemetry SDK or payload logging added; tests emit counts and timing only.

Local forced-exit and100k SQLite tests passed (100k records,12.16ms p95 combined indexed total/day-page,1827ms snapshot plus200 pages,765ms seed; Windows desktop only). Types/lint passed. Initial CI bulk insertion exposed repeated pg_timezone_names generation per UTC record; forward migration010 adds the semantically equivalent UTC fast path and preserves catalog validation for all other values, recorded-date consistency and immutable fields. Invalid-zone and wrong-UTC-date regressions remain required; benchmark setup SQL now has a90-second timeout. No constraints disabled.

Final combined source9b06540 passed backend34623108975 (20 real PostgreSQL/Auth tests plus34 pgTAP and connected browser flows), mobile34623108995 and package34623108976. Populated baseline upgrade completed in1807ms and preserved records/receipts/revision/membership while invalidating legacy sharing. The100k-record dataset spans10 days/10 authenticated accounts, with10k currently eligible records.100 simulated foreground clients issue one poll each staggered300ms across30 seconds using those10 sessions: p50=43ms, p95=50ms, max=60ms;100/100 HTTP responses correct, bounded25 rows and sanitized. These are disposable CI localhost timings, not deployed-region or native budgets. See t21-benchmark.json.
