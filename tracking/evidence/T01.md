# T01: toolchain and bootstrap

Branch: `task/t01-toolchain-bootstrap`. Date: 2026-09-10.

Plan: scaffold Expo TypeScript under apps/mobile, add Router and a Cream & Brick placeholder, establish configuration and reproducible checks. Verify clean install, lint, types, tests, Expo doctor, bundle export, and an available rendering target. Preserve the root reference package.

Baseline: Windows PowerShell; Node 24.12.0; npm 11.6.2; Git 2.49.0.windows.1. `npm.cmd test` at root passed 29 reference tests. Python is unavailable; package Python check not run locally. No Android SDK, adb, Java, Docker, PostgreSQL or Maestro found on PATH; no Android SDK at the default local path. Chrome is installed.

Scaffold: create-expo-app 4.0.0, blank-typescript 57.0.23; Expo 57.0.21, React Native 0.86.3, React 19.2.3, TypeScript 6.0.3. Read official SDK 57 and Router documentation. Windows command wrappers for npx-installed binaries returned Access denied; direct Node CLI invocation worked. PowerShell npm.ps1 is disabled, so use npm.cmd.

Verification results will be appended after execution. Native review requires a configured Android emulator/device and JDK/SDK, or an authorized EAS development build with a physical device. Browser evidence cannot satisfy that gate.

Results: npm.cmd ci passed (813 packages). Mobile lint/typecheck, 2 unit tests and 1 real Expo config integration test passed. Expo Doctor 1.20.4 passed 21/21 checks. Root contract syntax passed. Full export resolved native modules but Hermes process launch failed with spawn EPERM; native bytecode gate remains blocked. Web export passed and Chrome/Playwright 1.63.0 rendered the 390x844 screen without page errors; see t01-bootstrap-web.png. No native device verification. npm audit reports 13 moderate transitive findings (Expo/Xcode uuid and Router query-string); no incompatible audit-force downgrade applied. ESLint 9.39.5 follows Expo 57 compatibility; upstream marks it deprecated.

Updated the package validator to exclude dependencies/generated folders now that a mobile app exists; Python execution is unavailable here, so CI must verify that check.

T02/T03 may proceed independently of the missing native gate: component rendering and real host SQLite repository checks do not require a native binary. This exception does not mark T01 verified.
