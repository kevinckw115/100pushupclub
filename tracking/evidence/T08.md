# T08: private server schema and authorization

Branch task/t08-server-security. Date 2026-09-10.

Plan: private PostgreSQL schema, explicit grants/revokes and RLS, checked permanent-session bootstrap/get-profile functions, constraints and indexes. Verify fresh disposable Supabase reset, pgTAP grants/RLS, real Auth JWTs, bootstrap replay, forged ownership denial, suspension reauthorization and a two-connection alias uniqueness race.

Toolchain: Node24.12.0; Supabase CLI2.117.0 and pg8.23.0 installed in tools/backend with their own lockfile. The mobile app retains its one lockfile. Backend dependency audit reports zero vulnerabilities. Windows blocks the CLI binary with spawn EPERM; WSL reports not installed and Docker is unavailable. Local database tests have not run. GitHub Actions is configured to run the disposable stack and tests without hosted/production credentials.

All private tables enable RLS and have no anon/authenticated CRUD grants. Definer functions use an empty search_path and derive identity from auth.uid plus verified permanent Auth/account checks. Profile bootstrapping and its operation receipt share a transaction. ADR-001 updates API/data/sync/TypeScript envelopes together. The World-only local seed contains no users or check-ins.

CI results and any fixes will be recorded after execution. No hosted backend is linked or deployed.

Verified at 47c97c3 in GitHub Actions: fresh Supabase reset, all 7 pgTAP assertions, real A/B JWT authorization, alias lock race, invalid check-in writes, deletion reauthorization, and injected bootstrap receipt failure with complete rollback passed. Backend run: https://github.com/kevinckw115/100pushupclub/actions/runs/34541574331 . Mobile full export/checks and package Python/reference checks also passed at this commit. Native and hosted integration remain separate gates.
