import { AppScreen, Header, Copy, Notice } from '../../src/components/ui';
export default function Circles() {
  return <AppScreen><Header /><Copy variant="title">Circles</Copy><Copy>A small group, showing up together.</Copy><Notice>Circles need an account. Account setup is not available in this build yet.</Notice></AppScreen>;
}
